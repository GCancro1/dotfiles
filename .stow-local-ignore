# Directories to ignore (blacklist approach)
# Add patterns here to exclude from stow sync
# These are Perl regular expressions, one per line

# yt-dlp - contains machine-specific config
# Note: regex matches the basename when no / is present
^yt-dlp$

# Package manifests - used by bootstrap script, not dotfiles
^packages\.txt$
^packages-dev\.txt$
^packages-hyprland\.txt$
