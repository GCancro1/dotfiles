# ── Animation Speed ─────────────────────────────────────────────────
# All animation speeds are set together for easy adjustment.
#
# Quick-change presets (replace all 11 animation speed values):
#   Normal (1x): 6, 4, 6, 6, 4, 6, 6, 4, 6, 6, 6
#   Fast   (2x): 3, 2, 3, 3, 2, 3, 3, 2, 3, 3, 3  ← current
#   Faster (3x): 2, 1.33, 2, 2, 1.33, 2, 2, 1.33, 2, 2, 2
#   Ultra  (4x): 1.5, 1, 1.5, 1.5, 1, 1.5, 1.5, 1, 1.5, 1.5, 1.5
#
# Speeds order: layersIn, layersOut, fadeLayers, windowsIn, windowsOut,
#               windowsMove, workspaces, specialWorkspace, fade, fadeDim, border
# ────────────────────────────────────────────────────────────────────

animations {
    enabled = true

    # Animation curves
    bezier = specialWorkSwitch, 0.05, 0.7, 0.1, 1
    bezier = emphasizedAccel, 0.3, 0, 0.8, 0.15
    bezier = emphasizedDecel, 0.05, 0.7, 0.1, 1
    bezier = standard, 0.2, 0, 0, 1

    # Animation configs — currently set to 2x speed
    animation = layersIn, 1, 3, emphasizedDecel, slide
    animation = layersOut, 1, 2, emphasizedAccel, slide
    animation = fadeLayers, 1, 3, standard

    animation = windowsIn, 1, 3, emphasizedDecel
    animation = windowsOut, 1, 2, emphasizedAccel
    animation = windowsMove, 1, 3, standard
    animation = workspaces, 1, 3, standard

    animation = specialWorkspace, 1, 2, specialWorkSwitch, slidefadevert 15%

    animation = fade, 1, 3, standard
    animation = fadeDim, 1, 3, standard
    animation = border, 1, 3, standard
}
